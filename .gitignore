*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
/app/build/

## Signing — never commit the keystore or its credentials
/keystore/
/keystore.properties
*.jks
*.keystore

## Google Play — never commit the service-account key
/play-service-account.json
